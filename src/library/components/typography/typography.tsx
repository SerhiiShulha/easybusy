import styled from '@emotion/styled'
import { mq } from '../../constants/styles'

export const SectionTitle = styled.h3`
  font-size: 4rem;
  font-weight: 800;
  line-height: 5rem;

  ${mq.xs} {
    font-size: 2.5rem;
    line-height: 1;
  }
`
